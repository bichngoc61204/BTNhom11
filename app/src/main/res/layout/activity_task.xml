<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <TextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="NHÓM 11"
        android:textStyle="bold"
        android:textSize="30sp"
        android:layout_margin="20dp"
        android:layout_centerHorizontal="true"
        />

    <TextView
        android:id="@+id/header_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Danh sách các task"
        android:textStyle="bold"
        android:textSize="30sp"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/title"/>

    <!-- LinearLayout chứa 2 EditText và Button -->
    <LinearLayout
        android:id="@+id/layout_task_input"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_below="@+id/header_title"
        android:layout_marginTop="10dp"
        android:padding="10dp">

        <!-- EditText cho việc nhập task -->
        <EditText
            android:id="@+id/edit_task"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:hint="Nhập công việc"
            android:layout_marginEnd="10dp"/>

        <!-- Button thêm task -->
        <Button
            android:id="@+id/button_add_task"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Thêm"
            android:background="@drawable/button_pressed"/>
    </LinearLayout>

    <!-- RecyclerView hiển thị danh sách task -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rcv_task"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/layout_task_input"
        android:layout_marginTop="10dp"/>

</RelativeLayout>
